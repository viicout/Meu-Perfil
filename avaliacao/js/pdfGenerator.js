// pdfGenerator.js
// Gera PDF "limpo" usando window.print() - o CSS @media print já faz o resto.
// Aqui apenas centralizamos a ação para futuras melhorias.

export function printProgram() {
  window.print();
}
